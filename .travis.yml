language: java
jdk:
- openjdk8

services:
  - docker

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

script:
  - ./gradlew downloadCtpApiZipFile
  - ./gradlew :languages:javalang:builder-renderer:java-builder-client:test
  - ./gradlew check