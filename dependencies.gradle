ext {
    versions = [
            annotations: '1.3.2',
            assertj: '3.16.0',
            commonmark: '0.14.0',
            directoryWatcher: '0.15.0',
            emfjsonJackson: '1.3.0',
            guava: '29.0-jre',
            junit: '5.6.0',
            jackson: '2.12.4',
            javaxValidation: '2.0.1.Final',
            kotlin: kotlinVersion,
            picocli: '4.6.1',
            rmf: '0.2.0-20211203200453',
            rxjava: '3.1.2',
            slf4j: '1.7.25',
            spock: '2.0-groovy-3.0',
            swaggerParser: '2.0.28'
    ]

    commons = [
            io: 'commons-io:commons-io:2.11.0',
            lang3: 'org.apache.commons:commons-lang3:3.10',
            text: 'org.apache.commons:commons-text:1.9',
    ]

    commonmark = [
            commonmark: "com.atlassian.commonmark:commonmark:${versions.commonmark}"
    ]

    google = [
            guava: "com.google.guava:guava:${versions.guava}"
    ]

    rmf = [
            raml_model: "com.commercetools.rmf:raml-model:${versions.rmf}"
    ]

    orgkotlin = [
            stdlib: "org.jetbrains.kotlin:kotlin-stdlib-jdk8:${versions.kotlin}",
            reflect: "org.jetbrains.kotlin:kotlin-reflect:${versions.kotlin}"
    ]

    jackson_core = [
            databind: "com.fasterxml.jackson.core:jackson-databind:${versions.jackson}",
            dataformat_xml: "com.fasterxml.jackson.dataformat:jackson-dataformat-xml:${versions.jackson}",
            kotlin: "com.fasterxml.jackson.module:jackson-module-kotlin:${versions.jackson}"
    ]

    picocli = [
            cli: "info.picocli:picocli:${versions.picocli}",
            codegen: "info.picocli:picocli-codegen:${versions.picocli}",
    ]

    directory_watcher = [
            watcher: "io.methvin:directory-watcher:${versions.directoryWatcher}"
    ]
    emf = [
            jackson: "org.emfjson:emfjson-jackson:${versions.emfjsonJackson}"
    ]

    slf4j = [
            slf4j: "org.slf4j:slf4j-simple:${versions.slf4j}"
    ]

    rxjava = [
            rxjava: "io.reactivex.rxjava3:rxjava:${versions.rxjava}"
    ]

    assertj = [
            core: 'org.assertj:assertj-core:3.16.0'
    ]

    junit = [
            junit: 'org.junit.jupiter:junit-jupiter:5.6.0',
            dataprovider: 'com.tngtech.junit.dataprovider:junit-jupiter-dataprovider:2.6'
    ]

    spockframework = [
            spock_core: "org.spockframework:spock-core:${versions.spock}"
    ]

    javax = [
            annotations: "javax.annotation:javax.annotation-api:${versions.annotations}",
            validation: "javax.validation:validation-api:${versions.javaxValidation}"
    ]
    swagger = [
            swagger_parser: "io.swagger.parser.v3:swagger-parser:${versions.swaggerParser}"
    ]
}
